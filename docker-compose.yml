app:
  build: ./
  working_dir: /project
  links:
    - mongo
  volumes:
    - ./:/project
  command: forever app.js
  environment:
    NODE_ENV: 'production'
  ports:
    - "5000:5000"

mongo:
  image: mongo:latest